<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.homework.mapper.CourseMapper">

    <select id="list" resultType="com.example.homework.entity.Course">
        SELECT * FROM course
        <where>
            <if test="teacherId != null">AND teacher_id = #{teacherId}</if>
            <if test="clazzId != null">AND clazz_id = #{clazzId}</if>
        </where>
    </select>

    <insert id="add" parameterType="com.example.homework.entity.Course">
        INSERT INTO course (course_name, teacher_id, clazz_id, create_time, update_time)
        VALUES (#{courseName}, #{teacherId}, #{clazzId}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.example.homework.entity.Course">
        UPDATE course
        <set>
            <if test="courseName != null">course_name = #{courseName},</if>
            <if test="teacherId != null">teacher_id = #{teacherId},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <select id="hasHomework" resultType="boolean">
        SELECT COUNT(*) > 0 FROM homework WHERE course_id = #{id}
    </select>

    <delete id="delete">
        DELETE FROM course WHERE id = #{id}
    </delete>
</mapper>