<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.homework.mapper.HomeworkSubmitDetailMapper">

    <!-- 根据提交ID 查询该学生所有题目的答案 -->
    <select id="selectBySubmitId" parameterType="long" resultType="com.example.homework.entity.HomeworkSubmitDetail">
        SELECT * FROM homework_submit_detail WHERE submit_id=#{submitId}
    </select>

    <!-- 保存学生某一道题的答案 -->
    <insert id="insert" parameterType="com.example.homework.entity.HomeworkSubmitDetail">
        INSERT INTO homework_submit_detail (
            submit_id,
            question_id,
            student_answer,
            score,
            remark,
            question_submit_status
        ) VALUES (
                     #{submitId},
                     #{questionId},
                     #{studentAnswer},
                     #{score},
                     #{remark},
                     #{questionSubmitStatus}
                 )
    </insert>

    <update id="updateScore" parameterType="com.example.homework.entity.HomeworkSubmitDetail">
        UPDATE homework_submit_detail
        SET score=#{score}, remark=#{remark}
        WHERE id=#{id}
    </update>

    <select id="selectBySubmitAndQuestion" parameterType="map" resultType="com.example.homework.entity.HomeworkSubmitDetail">
        SELECT * FROM homework_submit_detail
        WHERE submit_id = #{submitId} AND question_id = #{questionId}
    </select>

    <update id="updateById" parameterType="com.example.homework.entity.HomeworkSubmitDetail">
        UPDATE homework_submit_detail
        SET student_answer = #{studentAnswer},
            question_submit_status = #{questionSubmitStatus},
            score = #{score}
        WHERE id = #{id}
    </update>

</mapper>